using namespace std;

enum Postion {COLLECTION, INTO_RECYCLING, COLOUR_SENSOR, INTO_NEST};

// Sorting motor class
class SortingMotor {
public:
    void switch_position(Postion);
    Position current_position;
private:
    void move(int):
}
