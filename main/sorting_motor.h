using namespace std;

enum Postion {NORMAL, INTO_RECYCLING, INTO_NEST, RELEASE LATCH}
// Sorting motor class
class SortingMotor {
public:
    void switch_position(Postion);
    Position current_position;
    
}
