Main;

do{
  Navigate to:
  *IR transmitter*;
  Receive the signal;
  <Set delivery_point>;
  <Set eggs_required>;
  Navigate to:
  *Egg collection plate*;
  call Egg collection and sorting;
  Navigate to:
  *delivery point*;
  Enable egg delivery motor;
  
}while (!all four nests delivered);

Load destination :
  *Recycling point*;
recycle;
return Finish;
