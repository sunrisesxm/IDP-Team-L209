Main;

do{
  Load destination :
  *IR transmitter*;
  call Forward line control;
  Receive the signal;
  <Set delivery_point>;
  <Set eggs_required>;
  Load destination :
  *Egg collection plate*;
  call Forward line control;
  call Egg collection and sorting;
  Load destination :
  *delivery point*;
  call Forward line control;
  Enable egg delivery motor;
  
}while (!all four nests delivered);

Load destination :
  *Recycling point*;
recycle;
return Finish;
