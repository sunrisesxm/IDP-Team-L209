Disaster Recovery;
np:
normal procedure;


branch (A){
  faltu analogue/digital signal;
  reset everything from controller;
  input test (again);
  if (passed) goto np;
  else return STOP

}
}

branch (B){

if (too close to the wall){
  emegency stop;
  reverse to the nearest junction;
  carry-on navigation;
  goto np;
}
}

branch (B){

if (motor_speed << set_speed){
  emegency stop;
  flash LEDs;
  a+=1;
  //stop and start again;
  if (a>=2) {
    //something is stuck, stop.
    return STOP!;}
  goto np
}
}



